<section id="about" class="relative w-full bg-black overflow-hidden">
  <!-- Background gradient -->
  <div
    class="absolute inset-0 bg-[radial-gradient(circle_at_50%_50%,rgba(120,119,198,0.06),rgba(0,0,0,0)_70%)]"
  >
  </div>

  <div class="relative z-10">

    {/* Section 1: Image left, text right */}
    <div class="container mx-auto px-6 md:px-12 py-20 md:py-32">
      <div class="max-w-7xl mx-auto">
        <div class="grid lg:grid-cols-2 gap-12 lg:gap-16 items-center">

          {/* Image */}
          <div class="relative group order-2 lg:order-1">
            <div class="relative overflow-hidden rounded-2xl shadow-2xl">
              <img
                src="/band-stage.PNG"
                alt="Back To The Start performing live"
                width="1920"
                height="1080"
                loading="lazy"
                class="w-full h-full object-cover transform group-hover:scale-105 transition-transform duration-700"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
            </div>
          </div>

          {/* Text */}
          <div class="space-y-8 order-1 lg:order-2">
            <div class="space-y-4">
              <div class="inline-block">
                <div class="h-1 w-16 bg-gradient-to-r from-blue-500 to-purple-500 rounded-full"></div>
              </div>
              <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold text-white tracking-tight leading-tight">
                The Early Years
              </h2>
            </div>
            <div class="space-y-6 text-lg md:text-xl text-white/70 leading-relaxed">
              <p>
                Back to the Start is a tribute to Coldplay's earliest and most
                iconic era, capturing the raw emotion and atmospheric beauty of <em
                  class="text-white/85">Parachutes</em
                >, <em class="text-white/85">A Rush of Blood to the Head</em>, and <em class="text-white/85">X&Y</em>.
              </p>
              <p>
                With a deep reverence for the original sound, we deliver an
                immersive experience that takes audiences on a journey through
                Coldplay's golden years—from the intimate melancholy of <strong
                  class="text-white/90">"Sparks"</strong
                > to the soaring anthems like <strong class="text-white/90">"Clocks"</strong> and <strong
                  class="text-white/90">"Fix You."</strong
                >
              </p>
            </div>
          </div>

        </div>
      </div>
    </div>

    {/* Divider */}
    <div class="h-px bg-gradient-to-r from-transparent via-white/10 to-transparent"></div>

    {/* Section 2: Text left, image right */}
    <div class="container mx-auto px-6 md:px-12 py-20 md:py-32">
      <div class="max-w-7xl mx-auto">
        <div class="grid lg:grid-cols-2 gap-12 lg:gap-16 items-center">

          {/* Text */}
          <div class="space-y-8">
            <div class="space-y-4">
              <div class="inline-block">
                <div class="h-1 w-16 bg-gradient-to-r from-purple-500 to-pink-500 rounded-full"></div>
              </div>
              <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold text-white tracking-tight leading-tight">
                The Musicians
              </h2>
            </div>
            <div class="space-y-6 text-lg md:text-xl text-white/70 leading-relaxed">
              <p>
                Led by vocalist and multi-instrumentalist <span class="text-white/90 font-semibold">Scott Ivey</span>, the band
                features <span class="text-white/90 font-semibold">Eric Johnson</span> on guitar, <span class="text-white/90 font-semibold">Daniel Ellis</span> on bass, and <span class="text-white/90 font-semibold">Danny
                Sullivan</span> on drums.
              </p>
              <p>
                Together, we channel the spirit, tone, and
                passion that made Coldplay a global phenomenon—before the stadium
                synths and pop beats. Every note, every arrangement, every moment is crafted to transport you back to those unforgettable early albums.
              </p>
            </div>
          </div>

          {/* Image */}
          <div class="relative group">
            <div class="relative overflow-hidden rounded-2xl shadow-2xl">
              <img
                src="/band-bw.jpg"
                alt="Back To The Start band members"
                width="1920"
                height="1080"
                loading="lazy"
                class="w-full h-full object-cover transform group-hover:scale-105 transition-transform duration-700"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
            </div>
          </div>

        </div>
      </div>
    </div>

    {/* Divider */}
    <div class="h-px bg-gradient-to-r from-transparent via-white/10 to-transparent"></div>

    {/* Photo Gallery Section - Slideshow */}
    <div class="relative py-20 md:py-32 overflow-hidden">
      {/* Diagonal background element */}
      <div class="absolute inset-0 bg-gradient-to-br from-yellow-400/5 via-transparent to-purple-500/5 transform -skew-y-3 origin-top-left"></div>

      <div class="container mx-auto px-6 md:px-12 relative z-10">
        <div class="max-w-7xl mx-auto">

          {/* Header */}
          <div class="mb-16 md:mb-20">
            <div class="max-w-3xl mx-auto text-center">
              <div class="inline-block mb-4">
                <div class="h-1 w-20 bg-gradient-to-r from-yellow-400 to-yellow-300 rounded-full mx-auto"></div>
              </div>
              <h3 class="text-5xl md:text-6xl lg:text-7xl font-bold text-white tracking-tight leading-none mb-6">
                Photo<br/>Gallery
              </h3>
              <p class="text-xl md:text-2xl text-white/60 font-light">
                Moments from our performances
              </p>
            </div>
          </div>

          {/* Slideshow Container */}
          <div class="relative w-full">

            {/* Slides */}
            <div id="slideshow-container" class="relative w-full overflow-hidden rounded-3xl shadow-2xl h-[400px] md:h-[500px] lg:h-[600px]">
              <div class="slide active">
                <img src="/gallery1.png" alt="Back To The Start" class="w-full h-full object-cover">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent"></div>
              </div>
              <div class="slide">
                <img src="/gallery2.png" alt="Back To The Start" class="w-full h-full object-cover">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent"></div>
              </div>
              <div class="slide">
                <img src="/gallery3.png" alt="Back To The Start" class="w-full h-full object-cover">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent"></div>
              </div>
              <div class="slide">
                <img src="/gallery4.png" alt="Back To The Start" class="w-full h-full object-cover">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent"></div>
              </div>
              <div class="slide">
                <img src="/gallery5.png" alt="Back To The Start" class="w-full h-full object-cover">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent"></div>
              </div>
              <div class="slide">
                <img src="/gallery6.png" alt="Back To The Start" class="w-full h-full object-cover">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent"></div>
              </div>
              <div class="slide">
                <img src="/gallery7.png" alt="Back To The Start" class="w-full h-full object-cover">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent"></div>
              </div>
              <div class="slide">
                <img src="/gallery8.png" alt="Back To The Start" class="w-full h-full object-cover">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent"></div>
              </div>
              <div class="slide">
                <img src="/gallery9.png" alt="Back To The Start" class="w-full h-full object-cover">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent"></div>
              </div>
              <div class="slide">
                <img src="/gallery10.png" alt="Back To The Start" class="w-full h-full object-cover">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent"></div>
              </div>
              <div class="slide">
                <img src="/gallery11.png" alt="Back To The Start" class="w-full h-full object-cover">
                <div class="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent"></div>
              </div>
            </div>

            {/* Navigation Arrows */}
            <button id="prev-slide" class="absolute left-4 md:left-8 top-1/2 -translate-y-1/2 z-20 w-12 h-12 md:w-14 md:h-14 rounded-full bg-black/50 hover:bg-black/70 backdrop-blur-sm flex items-center justify-center text-white transition-all duration-300 hover:scale-110 border border-white/20" aria-label="Previous slide">
              <i class="fas fa-chevron-left text-lg"></i>
            </button>
            <button id="next-slide" class="absolute right-4 md:right-8 top-1/2 -translate-y-1/2 z-20 w-12 h-12 md:w-14 md:h-14 rounded-full bg-black/50 hover:bg-black/70 backdrop-blur-sm flex items-center justify-center text-white transition-all duration-300 hover:scale-110 border border-white/20" aria-label="Next slide">
              <i class="fas fa-chevron-right text-lg"></i>
            </button>

            {/* Dots Indicator */}
            <div id="dots-container" class="absolute bottom-6 left-1/2 -translate-x-1/2 z-20 flex gap-2"></div>

          </div>
        </div>
      </div>
    </div>

    <style>
      .slide {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        opacity: 0;
        transition: opacity 0.8s ease-in-out;
      }

      .slide.active {
        opacity: 1;
      }

      .dot {
        width: 10px;
        height: 10px;
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.3);
        cursor: pointer;
        transition: all 0.3s ease;
      }

      .dot.active {
        background: #facc15;
        width: 32px;
        border-radius: 5px;
      }
    </style>

    <script>
      let currentSlide = 0;
      const slides = document.querySelectorAll('.slide');
      const totalSlides = slides.length;
      let autoplayInterval: number;

      // Create dots
      const dotsContainer = document.getElementById('dots-container');
      if (dotsContainer) {
        for (let i = 0; i < totalSlides; i++) {
          const dot = document.createElement('div');
          dot.classList.add('dot');
          if (i === 0) dot.classList.add('active');
          dot.addEventListener('click', () => goToSlide(i));
          dotsContainer.appendChild(dot);
        }
      }

      function showSlide(n: number) {
        slides.forEach(slide => slide.classList.remove('active'));
        document.querySelectorAll('.dot').forEach(dot => dot.classList.remove('active'));

        currentSlide = (n + totalSlides) % totalSlides;
        slides[currentSlide].classList.add('active');
        document.querySelectorAll('.dot')[currentSlide].classList.add('active');
      }

      function nextSlide() {
        showSlide(currentSlide + 1);
      }

      function prevSlide() {
        showSlide(currentSlide - 1);
      }

      function goToSlide(n: number) {
        showSlide(n);
        resetAutoplay();
      }

      function startAutoplay() {
        autoplayInterval = window.setInterval(nextSlide, 5000);
      }

      function resetAutoplay() {
        clearInterval(autoplayInterval);
        startAutoplay();
      }

      // Event listeners
      document.getElementById('next-slide')?.addEventListener('click', () => {
        nextSlide();
        resetAutoplay();
      });

      document.getElementById('prev-slide')?.addEventListener('click', () => {
        prevSlide();
        resetAutoplay();
      });

      // Keyboard navigation
      document.addEventListener('keydown', (e) => {
        if (e.key === 'ArrowLeft') {
          prevSlide();
          resetAutoplay();
        } else if (e.key === 'ArrowRight') {
          nextSlide();
          resetAutoplay();
        }
      });

      // Start autoplay
      startAutoplay();

      // Pause on hover
      const container = document.getElementById('slideshow-container');
      container?.addEventListener('mouseenter', () => clearInterval(autoplayInterval));
      container?.addEventListener('mouseleave', () => startAutoplay());
    </script>

  </div>
</section>
