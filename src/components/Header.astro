<head>
  {/* Preload critical assets */}
  <link rel="preload" as="video" href="/background-video.mp4" type="video/mp4" />
  <link rel="preload" as="image" href="/logo.png" type="image/png" />
  <link rel="preload" as="image" href="/header-text.png" type="image/png" />
</head>

<section
  class="relative w-full h-screen overflow-hidden"
>
  {/* Full-width hero video */}
  <div class="absolute inset-0">
    <video
      src="/background-dance.mp4"
      autoplay
      loop
      muted
      playsinline
      class="w-full h-full object-cover object-center"
    >
    </video>
  </div>

  {/* Logo in top right */}
  <div class="absolute top-6 right-6 md:top-8 md:right-8 z-20">
    <img
      src="/logo.png"
      alt="Back To The Start logo"
      class="h-12 md:h-16 w-auto opacity-90"
    />
  </div>

  {/* Bottom transparent bar with content - like the screenshot */}
  <div class="absolute inset-x-0 bottom-0 z-10 bg-black/60 backdrop-blur-md">
    <div class="container mx-auto px-6 md:px-12 py-8 md:py-10">
      <div class="flex flex-col md:flex-row items-center md:items-end justify-between gap-6">

        {/* Left side - Header text image (centered on mobile, left on desktop) */}
        <div class="text-center md:text-left">
          <img
            src="/header-text.png"
            alt="Back To The Start - A Tribute to Coldplay 2000-2005"
            class="w-auto h-auto max-w-[280px] md:max-w-[320px] lg:max-w-[400px] mx-auto md:mx-0"
          />
        </div>

        {/* Right side - CTA Button */}
        <div class="flex-shrink-0">
          <a
            href="#book-us"
            class="inline-flex items-center gap-3 px-8 md:px-10 py-3.5 md:py-4 bg-yellow-400 hover:bg-yellow-300 text-black font-bold text-base md:text-lg rounded-full transition-all duration-300 shadow-2xl hover:shadow-yellow-400/50 hover:scale-105 whitespace-nowrap"
            onclick="document.getElementById('book-us').scrollIntoView({behavior: 'smooth', block: 'start'}); return false;"
          >
            <span>Book Now</span>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
            </svg>
          </a>
        </div>

      </div>
    </div>
  </div>
</section>

<style>
  html {
    scroll-behavior: smooth;
  }
</style>
